const moja = document.querySelector('.img-hamburger');
const hiddenDiv = document.querySelector('.mob-overlay');

moja.addEventListener('click', () => {

    hiddenDiv.style.display = 'flex';
    
});